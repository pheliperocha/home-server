apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nodered.name }}
  labels:
    {{- include "nodered.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 1880
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodered.selectorLabels" . | nindent 4 }}