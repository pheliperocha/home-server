homeassistant:
  enabled: false
  name: homeassistant
  localHostname: homeassistant.local
  publicHostname: homeassistant.pheliperocha.com
  replicaCount: 1
  serviceAccount:
    create: true
    annotations: {}
  tunnel:
    enabled: false
    cloudflare_name: home_assistant

nodered:
  enabled: false
  name: nodered
  localHostname: nodered.local
  replicaCount: 1
  serviceAccount:
    create: true
    annotations: {}

drone_launcher:
  enabled: false
  name: drone-launcher
  port: 80
  publicHostname: drone.pheliperocha.com
  localHostname: drone.local
  tunnel:
    enabled: false
    cloudflare_name: drone

qbittorrent:
  enabled: false
  name: qbittorrent
  localHostname: qbittorrent.local
  replicaCount: 1
  serviceAccount:
    annotations: {}

kubernetesDashboard:
  enabled: false
  name: kubernetes-dashboard
  localHostname: k8s.local

k8up:
  enabled: false

postgres-finance-project-staging:
  enabled: true
  namespace: finance-project-staging

postgres-finance-project-production:
  enabled: false

# postgresql:
    # TODO: Custom username
    # TODO: Custom password
    # TODO: Custom database
    # TODO: primary.annotations for k8up backup