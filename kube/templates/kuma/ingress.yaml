{{- if .Values.kuma.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.kuma.name }}-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - hosts:
        - {{ .Values.kuma.ingress.localHostname }}
      secretName: {{ .Values.kuma.name }}-staging-ingress-tls
  rules:
    - host: {{ .Values.kuma.ingress.localHostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.kuma.name }}-service
                port:
                  number: {{ .Values.kuma.port }}
{{- end }}