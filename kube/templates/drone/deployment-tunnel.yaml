---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "tunnel-{{ .Values.drone_launcher.name }}"
  labels:
    {{- include "drone_launcher.labels" . | nindent 4 }}
    tunnel: "tunnel-{{ .Values.drone_launcher.name }}"
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: "tunnel-{{ .Values.drone_launcher.name }}"
      app.kubernetes.io/instance: {{ .Release.Name }}
      tunnel: "tunnel-{{ .Values.drone_launcher.name }}"
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "tunnel-{{ .Values.drone_launcher.name }}"
        app.kubernetes.io/instance: {{ .Release.Name }}
        tunnel: "tunnel-{{ .Values.drone_launcher.name }}"
    spec:
      containers:
      - name: "tunnel-{{ .Values.drone_launcher.name }}"
        image: cloudflare/cloudflared:latest
        args:
        - tunnel
        # Points cloudflared to the config file, which configures what
        # cloudflared will actually do. This file is created by a ConfigMap
        # below.
        - --config
        - /etc/cloudflared/config/config.yaml
        - run
        livenessProbe:
          httpGet:
            # Cloudflared has a /ready endpoint which returns 200 if and only if
            # it has an active connection to the edge.
            path: /ready
            port: 2000
          failureThreshold: 1
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: "tunnel-{{ .Values.drone_launcher.name }}-config"
          mountPath: /etc/cloudflared/config
          readOnly: true
        # Each tunnel has an associated "credentials file" which authorizes machines
        # to run the tunnel. cloudflared will read this file from its local filesystem,
        # and it'll be stored in a k8s secret.
        - name: creds
          mountPath: /etc/cloudflared/creds
          readOnly: true
      volumes:
      - name: creds
        secret:
          secretName: "tunnel-{{ .Values.drone_launcher.name }}-credentials"
      - name: "tunnel-{{ .Values.drone_launcher.name }}-config"
        configMap:
          name: "tunnel-{{ .Values.drone_launcher.name }}-config"
          items:
          - key: config.yaml
            path: config.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "tunnel-{{ .Values.drone_launcher.name }}-config"
data:
  config.yaml: |
    tunnel: {{ .Values.drone_launcher.tunnel.cloudflare_name }}
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
    ingress:
    - hostname: {{ .Values.drone_launcher.publicHostname }}
      service: http://{{ .Values.drone_launcher.name }}-service.home-server.svc.cluster.local.:{{ .Values.drone_launcher.port }}
    - service: http_status:404
---
apiVersion: v1
kind: Secret
metadata:
  name: "tunnel-{{ .Values.drone_launcher.name }}-credentials"
type: Opaque
data:
  credentials.json: {{ .Values.drone_launcher.tunnel.credentials | toJson | b64enc }}
