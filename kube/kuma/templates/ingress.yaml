{{- if .Values.kuma.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.kuma.name }}-ingress
  {{- if .Values.ingress.certificate.enabled }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.ingress.certificate.clusterIssuerRef }}
  {{- end }}
spec:
  ingressClassName: public
  {{- if .Values.ingress.certificate.enabled }}
  tls:
    - hosts:
        - {{ .Values.kuma.ingress.localHostname }}
      secretName: {{ .Values.kuma.name }}-ingress-tls
  {{- end }}
  rules:
    - host: {{ .Values.kuma.ingress.localHostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.kuma.name }}-service
                port:
                  number: {{ .Values.kuma.port }}
{{- end }}