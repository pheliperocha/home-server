{{- if .Values.drone_launcher.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.drone_launcher.name }}-ingress
  {{- if .Values.ingress.certificate.enabled }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.ingress.certificate.clusterIssuerRef }}
  {{- end }}
spec:
  ingressClassName: public
  {{- if .Values.ingress.certificate.enabled }}
  tls:
    - hosts:
        - {{ .Values.drone_launcher.ingress.localHostname }}
      secretName: {{ .Values.drone_launcher.name }}-ingress-tls
  {{- end }}
  rules:
    - host: {{ .Values.drone_launcher.ingress.localHostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.drone_launcher.name }}-service
                port:
                  number: {{ .Values.drone_launcher.port }}
{{- end }}